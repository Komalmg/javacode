package javaclass;
import java.util.Vector;
import java.util.regex.Matcher;
import java.util.regex.Pattern;
public class hashset3 {
	



		
		public static void main(String[] args) {
			
			// marker interface??
					// :: 6+lpa ?? 
			
			// empty interface is called as marker interface.
			// use of MI: to make clones (duplicate objects or value of content)
			// REGEX
			// patterns: mob:
			// passs: (Gmail Pattern ) : dates yyyy/mm/dd
			// upper add == lower address;
			// delevery not able
			// strong, weak 
			
			// Regex: regular expressions
			// 1)  PAttern Class    2) MAtcher Class   3)PatternSyntax class
			Pattern p1=Pattern.compile("REHANX");  // Pattern class use to set pattern
			
			// to compare : Matcher
			Matcher mat=p1.matcher("REHAN"); // matcher use to match the pattern
			if(Matcher.mat)
			{
				System.out.println("Successsssss");
			}
		}
		
	}
